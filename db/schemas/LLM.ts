export interface ILLM {
    id : string; // random identifier
    name : string; // unique identifier
    inferenceEndpoint : string; // endpoint to interact with llm
    model : string; // model to interact with
}